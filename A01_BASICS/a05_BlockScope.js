//this is the block of code
{
    var a = 20;   // we access outside the block
    const b = 99; // we can not access outside the block
    let c = 10;   // we can not access outside the block
}

 console.log(a);
//console.log(b);
// console.log(c);
